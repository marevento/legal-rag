name: legal-rag
metadata:
  template: legal-rag

infra:
  provider: bicep
  path: infra
  module: main

services:
  backend:
    project: ./app/backend
    language: python
    host: containerapp
    docker:
      path: ./app/backend/Dockerfile
      context: ./app/backend
